services:
  race-server:
    build: .
    ports:
      - "${PORT:-9876}:${PORT:-9876}"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:${PORT:-9876}/health"]
      interval: 30s
      timeout: 5s
      retries: 3
